# EntityDataHub / SO 面板重构自审（本轮UI修复）

## 覆盖性检查
- [x] 每分类“新增配置包”按钮可见、可点。
- [x] 分类创建区不再被长输入框挤占。
- [x] 顶部 `刷新`/`全量校验`/`全部展开`/`全部折叠` 常驻可见。
- [x] 全量校验重复 address 输出具体 `EntityType/EntityIdToken` 冲突明细。
- [x] 名称校验（空、重复）仍有效。

## 风险与处理
1. 小窗口下布局挤压：创建区改为可换行，按钮设置最小宽度。
2. 提示信息可读性：空输入使用引导提示，错误使用红色提示。
3. 交互回归风险：保留原有单项校验/编辑/删除逻辑路径。
